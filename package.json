{
  "scripts": {
    "postinstall": "pip install -r requirements.txt && cd frontend && npm install && npm run build && cd ../backend && npm install && npm run build",
    "start": "node backend/dist/main.js"
  }
}
